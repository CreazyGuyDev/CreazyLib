package pl.creazy.creazylib.screen.menu;

import org.bukkit.entity.Player;

public class PlayerMenuHolder extends MenuHolder {
  public PlayerMenuHolder(PlayerMenu menu, MenuPage page, Player owner) {
    super(menu, page, owner);
  }
}
